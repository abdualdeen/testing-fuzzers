# rapidfuzz-radamsa
This repo has files that are used to compare the fuzzingbook fuzzer with the pyradamsa fuzzer.

## Run/Setup
`gcc --coverage -o triangle triangle.c & gcc --coverage -o calc calc.c & gcc --coverage -o doom_fast_inverse_sqrt doom_fast_inverse_sqrt.c`
